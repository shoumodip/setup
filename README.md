# Setup
Configuration files for Debian KDE

## Quick Start
```console
$ ./build.sh
```

## Notes
- Use `:MasonInstall clangd@21.1.0` because some small dicked loser decided to
  be a pedantic nerd about the styling options in the latest `clang-format`
