#!/bin/sh

set -e

list_dir() {
    for dir in $(ls "$1"); do
        ls "$1/$dir" | sed "s|^|$dir/|"
    done
}

open_dir() {
    cd "$1/$(list_dir "$1" | dmenu -l 10)"
    $TERMINAL -e tmux new-session $EDITOR .
}

open_dir ~/code
