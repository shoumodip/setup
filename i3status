general {
  interval = 5
}

order += "volume master"
order += "tztime local"

volume master {
  format = "♪: %volume"
  format_muted = "♪: muted (%volume)"
  device = "default"
}

tztime local {
  format = "%d/%m/%Y %I:%M%p"
}
